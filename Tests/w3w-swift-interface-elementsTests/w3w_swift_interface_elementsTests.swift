import XCTest
@testable import W3WSwiftInterfaceElements

final class w3w_swift_interface_elementsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(w3w_swift_interface_elements().text, "Hello, World!")
    }
}
